# ---------- Node.js / dependencies ----------
node_modules/

# ---------- Build artifacts ----------
dist/
src-tauri/target/

# ---------- Markdown docs policy ----------
# 有意为之：只把两份 README 作为对外文档入库，其余 Markdown（评审报告、会议纪要、
# 设计说明等）留在本地，避免仓库里散落半成品文档。
# 若需要把某份文档纳入版本库，在下面加一行 `!<文件名>` 显式豁免（注意 negate 规则
# 无法重新包含位于已被忽略目录内的文件，例如 docs/ 下的文件需要先放行目录）。
*.md
*.MD
!README.md
!README_EN.md

# ---------- Internal docs ----------
PRD.md
IMPLEMENTATION_NOTES.md
docs/

# ---------- Local environment and secrets ----------
.npmrc
.env
.env.local
.env.*.local
*.local.json
config.json

# ---------- Runtime cache / local app data ----------
EBWebView/
AppData/
Roaming/

# ---------- Development and test outputs ----------
*.log
*.err.log
*.out.log
cargo-check*.log
cargo-check*.err.log
cargo-check*.out.log
tauri-check*.log
tauri-check*.err.log
tauri-check*.out.log
tauri-dev*.log
tauri-dev*.err.log
tauri-dev*.out.log
tauri-open*.log
tauri-open*.err.log
tauri-open*.out.log
tauri-dev-run*.log
tauri-dev-run*.err.log
tauri-dev-run*.out.log
test-output/

# ---------- Temporary screenshots ----------
dashboard-mvp.png
settings-mvp.png
detail-mvp.png

# ---------- Temporary dirty files ----------
# 原先这里有 document.title / document.title' / [{ 三条，是早期调试时误创建的文件名，
# 经全仓库检索这些文件已不存在，条目一并删除。保留通用规则即可。
*.tmp
# 脱离 Tauri 做界面截图时临时放在项目根的预览页（Vite 会 serve 它），用完即删
_preview.html

# ---------- IDE / editors ----------
.vscode/
.idea/
*.swp
*.swo
*~
*.suo
*.ntvs*
*.njsproj
*.sln

# ---------- OS files ----------
Thumbs.db
Desktop.ini
.DS_Store
